<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cowsbulls">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cb"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/cb"
        android:supportsRtl="true"
        android:theme="@style/Theme.CowsBulls">
        <activity android:name=".twoplayerg"></activity>
        <activity android:name=".youvscreator" />
        <activity android:name=".twoplayer" />
        <activity android:name=".Start11" />
        <activity android:name=".Instructions" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>